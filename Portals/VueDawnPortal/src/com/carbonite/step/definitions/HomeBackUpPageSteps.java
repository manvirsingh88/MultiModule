package com.carbonite.step.definitions;

import org.testng.Assert;

import com.carbonite.Pages.HomeBackUpPage;
import com.carbonite.Pages.HomePage;
import com.carbonite.Pages.HomePage.Localisation;
import com.carbonite.base.CustomeBase;

import io.cucumber.java.en.Then;

public class HomeBackUpPageSteps extends CustomeBase {
	
	/*
	 * private CustomeBase base; private HomePage homepage; private HomeBackUpPage
	 * homeBackUpPage;
	 * 
	 * public HomeBackUpPageSteps(CustomeBase base){ this.base=base; homepage=new
	 * HomePage(base.wd, false); homeBackUpPage=homepage.homeBackUpPage(); }
	 * 
	 * @Then("Verify the title of page") public void verify_the_title_of_page() {
	 * String titleOfPage = homeBackUpPage.titleofThePage();
	 * Assert.assertEquals(titleOfPage, Localisation.PAGE_TITLE.get());
	 * 
	 * }
	 * 
	 * @Then("Verify the main Header Text") public void
	 * verify_the_main_header_text() { String header =
	 * homeBackUpPage.getMainHeaderText(); Assert.assertEquals(header,
	 * Localisation.MAIN_HEADER_TEXT.get()); }
	 * 
	 * @Then("Verify the Sub Header Text") public void verify_the_sub_header_text()
	 * { String subTitle = homeBackUpPage.getSubHeaderText().replace("\n", "");
	 * Assert.assertEquals(subTitle, Localisation.SUB_HEADER_TEXT.get()); }
	 * 
	 * @Then("Verify if image is present") public void verify_if_image_is_present()
	 * { Assert.assertTrue(homeBackUpPage.isImageDisplayed()); }
	 */

}
